// Initial State

const initialState = {
  clicks: 0,
};

// Set up reducers behaviour
const clickReducer = (state = initialState, action) => {
  // it does something
  return state;
};

export default clickReducer;
